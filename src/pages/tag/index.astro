---
import WritingsByYear from "@/components/WritingsByYear.astro";
import { DESCRIPTIONS, TITLES, URLS } from "@/constants";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getPublishedWritings, getTagsWithWritings, getWritingsByYear } from "@/utils/data";

const allWritings = await getPublishedWritings();

const tagsWithWritings = getTagsWithWritings(allWritings);

const writingsByYear = getWritingsByYear(allWritings);
---

<BaseLayout title={TITLES.tags} description={DESCRIPTIONS.tags}>
  <section class="container" aria-label="All tags">
    <div class="tags-meta">
      <h1 class="heading">Tags</h1>

      <ul class="tag-list" role="list">
        {
          tagsWithWritings.map(({ tag, writings }) => (
            <li class="tag-item">
              <a href={URLS.tag(tag)} class="tag-link">
                <span class="tag-name">{tag}</span>

                <span
                  class="tag-count"
                  aria-label={`${writings.length} ${writings.length === 1 ? "writing" : "writings"}`}
                >
                  {writings.length}
                </span>
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <WritingsByYear writingsByYear={writingsByYear} />
  </section>
</BaseLayout>

<style>
  /* -------------------------
   * Theme — Local Variables
   * ------------------------- */
  :root {
    --tags-padding-y: 8rem;
    --tags-content-width: 36rem;
    --tags-section-gap: 6rem;
  }

  /* -------------------------
   * Layout — Page Container
   * ------------------------- */
  .container {
    display: flex;
    flex-direction: column;
    gap: var(--tags-section-gap);
    margin-inline: auto;
    padding-block: var(--tags-padding-y);
    width: 100%;
    max-width: 48rem;
  }

  .tags-meta {
    margin-inline: auto;
    width: 100%;
    max-width: var(--tags-content-width);
  }

  /* -------------------------
   * Module — Heading
   * ------------------------- */
  .heading {
    margin-block-end: 1.5rem;
    font-weight: 700;
    font-size: 2.5rem;
    line-height: 1.2;
    font-family: var(--font-display);
    font-optical-sizing: auto;
  }

  /* -------------------------
   * Module — Tag List
   * ------------------------- */
  .tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  /* -------------------------
   * Module — Tag Link
   * Tag name left, count right; full-row click target
   * ------------------------- */
  .tag-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    border-radius: 9999px;
    background: var(--color-surface-100);
    padding-inline: 0.8rem 0.25rem;
    padding-block: 0.25rem;
    color: var(--color-surface-700);
    font-size: 0.875rem;
    line-height: 1.285;
    text-decoration: none;
    white-space: nowrap;
  }

  .tag-name::before {
    margin-inline-end: 1px;
    content: "#";
  }

  .tag-count {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    border-radius: 9999px;
    background: var(--color-bg);
    padding-inline: 0.5rem;
    padding-block: 0.25rem;
    min-width: 1.3rem;
    font-weight: 700;
    font-size: 0.625rem;
  }

  /* -------------------------
   * State — Interactive
   * ------------------------- */
  .tag-link:hover {
    background: var(--color-surface-200);
    color: var(--color-surface-900);
  }

  .tag-link:focus-visible {
    outline: var(--focus-outline);
    outline-offset: var(--focus-offset-sm);
    background: var(--color-surface-200);
    color: var(--color-surface-900);
  }

  /* -------------------------
   * State — Responsive
   * ------------------------- */
  @media (max-width: 64rem) {
    .container {
      padding-inline: 1.5rem;
    }

    :global(.writings-list) {
      margin-inline: auto;
      width: 100%;
      max-width: var(--tags-content-width);
    }
  }

  @media (max-width: 48rem) {
    :root {
      --tags-padding-y: 5rem;
      --tags-section-gap: 3rem;
    }
  }

  @media (max-width: 40rem) {
    .heading {
      font-size: 2rem;
    }
  }
</style>
